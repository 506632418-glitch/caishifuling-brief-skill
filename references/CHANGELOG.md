# 变更日志

## v1.9.1 (2026-09-24) — 防跳板块机器闸口（skill-rehab 复查：运行时行为缺陷）

- 🩺 **来源**：审核人实测反馈——执行中经常跳过某些板块，不按"确认完一个再下一个"推进。评估器 131/134 S 抓不到此症状（必要条件≠充分性），trace 级诊断定位四处根因：进度未物化（brief_data.json 无板块状态）、板块边界无校验锁（锁只管阶段0→1）、注入通道天然支持批量（脚本无板块概念）、未禁"一次答多板块"。
- ✅ **M1 注入闸口（确定性转移）**：`brief_inject.py` 新增 `--section N`——G1连续性/G2当前确认 两条硬规则机器校验 `progress`，违规输出 `ERROR: GATE-FAIL` 非零退码拒绝写入；重注入已确认板块且其后板块已确认 → `WARN` 放行（补正通道，对应 SKILL 🟡级"修改已确认版块"合法流程）。不带 `--section` = 旧版全量模式（阶段0初始化/历史数据兼容）。⚠️ 治疗中自纠：初版 G3 把超前确认一刀切硬拦，运行时实测抓到它会误杀补正重注入——降为 WARN 留痕（防漂移闸口不为造假设计，手改 progress 绕闸归纪律条款管）。全矩阵实测：3 种 GATE-FAIL + NO-PROGRESS + 范围校验 + 8板块顺序流 + 补正 WARN 通道 + skipped 豁免，全绿。
- ✅ **M2 进度物化**：`brief_data.json` 增 `progress:{current, confirmed[], skipped[]}` 必填字段；步骤B写操作模板先记进度账再 `--section {N}` 注入；阶段1加「🧷板块间校验锁」——动笔前读账定序号，账是唯一权威（防上下文压缩后失忆跳板）。
- ✅ **M3 铁律2升级**：每次回复只推进一个版块的一个步骤（A 或 B+C）；确认前禁把多板块字段写入 JSON；审核人显式要跳 → 记 `progress.skipped` 留痕+告知后果，不得静默乱序；熔断器补 GATE-FAIL/GATE-NO-PROGRESS 处置行（⛔禁手改 progress 绕闸）。
- 🔎 **未做（留档）**：M4 交付前 confirmed 覆盖 1..8 完整性检查（期望型，用户未选）；M5 字段→板块全量映射硬闸（反向型——f-s- 前缀跨战略/产品两层复用，维护成本>收益，闸口本意防漂移非防作弊）。
- 📝 版本号：v1.9.0 → v1.9.1（patch：行为护栏增强，字段口径零变动，历史 JSON 无 progress 时旧注入路径不受影响）

## v1.9.0 (2026-09-23) — brief模版逐板块确认：八板块口径全量落盘（审核人 Fancy 逐板块裁决）

- 🚶 **来源**：2026-09-22 起逐板块确认调整（板块一→八顺序走完），每处改动三处同步（对照表+SKILL.md+HTML工具），各板块经 grep 关键词计数 + `/tmp` 注入实测验证。
- ✅ **板块一·战略层（09-22）**：#4 产品规格改必填；战略角色四判据表（引流/利润/形象/卡位，判据+反面案例）落进对照表，`f-s-role-reason` 须引用判据条目+来源，禁"综合考量"空话。
- ✅ **板块二·心智层（09-23）**：#8 口号创作方向改必填（四方向判据+正反例见对照表，禁反向硬套）；#9 口播长度口径统一为**约1分钟、200-300字**；遗留分歧已裁决——"四大手法"=写作技巧层、"四大创作方向"=字段归类层，两套并存不冲突（SKILL 加术语映射段）。
- ✅ **板块三·产品层（09-23）**：#10 作用原理改**两段式**（痛点成因+痛点表现形式，不许只写产品怎么起效）；"核心技术"更名"**核心机理**"（字段 ID `f-ing-{n}-*` 不变、解析键双词向后兼容），三维度=机理名称/痛点成因/机理来源；#23-26 使用组四项改必填；感官/包装保持选填。
- ✅ **板块四·市场层（09-23）**：竞品 **≥2 条**且至少 2 条三字段填全；**竞品≠市面替代方案**（替代方案只进 POD 的 vs 栏）；竞品-优势须客观事实描述、禁贬低词。
- ✅ **板块五·人群（09-23）**：核心人群 **≥2 类**无上限；三要素不做标号格式但须**场景化叙事文案**；差异化铁律（多类人群禁共用句式/同组痛点词）下沉工具气泡+对照表。
- ✅ **板块六·场景（09-23）**：使用场景 **≥2 个必填**无上限；"谁"须身份可识别（能对回板块五人群）；A轨四段递进只作**写作引导**；每场景独立画面、禁复制人群句式换主语。
- ✅ **板块七·核心价值（09-23）**：POD **≥2 且四字段填全**按优先级排列；POP 保持选填但**硬上限≤3**；POD「vs竞品」须为板块四真实品牌（防编造打假风险）；RTB 证据形态**四选一**严禁编造；差异描述"一句话FAB"或"分列F/A/B"两种写法均合规；**同日补口径**——差异描述须**详述产品具体是如何改善这个痛点的（解决原理）**、以 **FAB 显式化结构**进行文案表述（呼应板块三作用原理的痛点链，FAB 是表述结构不是内容替代品）。
- ✅ **板块八·FAQ（09-23）**：#37/#38 改必填——Q/A **成对必填、≥2 组**无上限；问题须为客服/评论区/私信**真实高频问题**，禁编造问题凑数（=变相虚假宣传）；回答依据只取真实产品信息文件；禁忌人群问题引「注意事项」原文结论+建议咨询医师。
- 🔗 **HTML工具同步**：各板块 std 气泡/placeholder/必填`*`标记全部对齐上述口径；必填统计口径更新（版块总表：战略5/心智3/产品5+机理≥1/市场2条×3项/人群2+/场景2+/POD≥2×4/FAQ 2+组成对）。
- 📝 版本号：v1.8.0 → v1.9.0
- 🩺 **同日复查康复（2026-09-23）**：评估器 131/134 S 零降分复查中，完整性清单抓出 1 项基本型冲突——「典型适用字段」表 FAQ 行旧授权（AI 基于品类常见问题生成）与 FAQ 硬规则（禁编造问题）矛盾，已修为**半授权**（仅回答可生成，问题必须真实素材）；同步为板块八补 💡范例气泡（两组合规问答，标"形式示意"）。履历/诊断见工作区 `.goal/rehab-diff-v19/`。教训：**口径收紧后须反扫旧规则里授权该行为的例外清单**。

## v1.8.0 (2026-09-22) — skill-rehab 康复：确定性转移 + 运行时合规校验脚本化

- 🩺 **来源**：skill-rehab 康复流程（评估器 v4.2.0 诊断 108/134 → 治疗 → 复查 131/134 S）。只修基本型（死规矩9），期望型/兴奋型仅记录不实施。
- ✅ **占位符泄漏修复（P0）**：清除文档示例中的 `XXX`、`<input value="xxx">` 等模糊占位符（3处），改为可执行示例或省略号——防模型照抄进产物。
- ✅ **运行时行为校验脚本化（P0）**：新增 `scripts/check_brief_compliance.py`——HARD_BANNED 医疗禁用词/极限词 + WARN 待裁决词 + 占位符残留 + 免责声明检查，协议 PASS/FAIL/WARN/ERROR + 退出码 0/1/2。判定权在脚本，AI 不得豁免。
- ✅ **可机器化验收声明节（P0）**：SKILL.md 新增「✅ 可机器化验收」表格（brief_inject.py `OK:` 退出码0 / check_brief_compliance.py `PASS:` 退出码0）；步骤B 重建为 4 步链（更新JSON→注入→验通道1→跑合规脚本）。
- ✅ **路径可移植 + /tmp 清除**：所有 `/tmp/brief_data.json` 引用改为 `[输出目录]/brief_data.json`，加 ⛔ 禁放 /tmp 注记（防异地 cwd / 沙箱写失败）。
- ✅ **依赖声明**：frontmatter `dependencies: [] → [python3]`（防"依赖不声明"反模式）。
- ✅ **事件层护栏（hook）显式留AI理由**：禁止#8 补注不挂 hook 的理由（Git 仓库可回滚兜底、避免冗余 hook）——completeness.md hook 判定缺口闭环。
- 📄 **触发测试集 + README**：新增 `tests/trigger-cases.md`（P正例/N负例/B边界 + 真实路径校准回写）、`README.md`（安装/依赖/使用/文件清单/贡献）。
- 🔗 **案例文件名校准**：references 案例路径改为真机 ls 核对的实际文件名，注明"读失败先 ls 核对"。
- ⏸️ **未修项**：`l1_name_naming`（中文名，判为反向型——强改 kebab-case 会破坏品牌识别与触发习惯，越改越糟）→ BLOCKED.md 留裁决。
- 📝 版本号：v1.7.1 → v1.8.0

## v1.7.1 (2026-07-10) — POD 规则细化：FAB 显式化 + RTB↔POD 闭环

- 🔧 **POD 写作标准细化**（延续 v1.7.0 双轨框架，B轨·客观专业轨）：
  - **FAB 差异化描述必写且结构清晰**：要求用 `F（特征）→ A（优势）→ B（利益）` 三段说清差异点；明确"可用括号注释 FAB 含义"（如 `F（特征）：平均48℃±5℃恒温发热 → A（优势）：温度温和适中 → B（利益）：适合长时间佩戴`），括号注释可选但 FAB 三段逻辑必须完整
  - **RTB ↔ POD 闭环（必查）**：RTB 必须直接支撑对应 POD 宣称，形成"声称→证据"闭环；来源客观真实（优先第三方检测/公开文献/实测，禁编造）；引用典籍须"书名+原文引述+出处"三要素齐全且与 POD 利益对应，产品化小结句仍须收敛治疗性暗示
  - 自检清单新增 FAB 完整性 + RTB 闭环两项
- 🎯 **实战落地**：同步将上述标准应用到「红花当归蒸汽手腕带」brief 的三个 POD——desc 改为显式 FAB（带括号注释）、RTB 收敛为与本 POD 宣称对应的证据（POD1温度检测数据 / POD2本草典籍出处 / POD3穴位经络理论），重注入验证 71 字段落位
- 📝 版本号：v1.7.0 → v1.7.1

## v1.7.0 (2026-07-10) — 双轨文案风格规范固化

- 📐 **新增「双轨文案风格（核心分类标准）」章节**（置于文案风格规范最前，统管全版块）：
  - **A轨 · 画面叙事轨**：核心人群、使用场景 → 画面感、口语化、差异化
  - **B轨 · 客观专业轨**：FAQ、POD → 客观、专业、中性陈述
  - 明确两轨互斥；补入"结构范式 ≠ 内容范本"铁律（给句式模板只套结构、每类目仍须差异化填充）
- 🔧 **各版块风格条款对齐双轨**：
  - 人群描述：标注【A轨】，强化"口语化/有画面感"与"每类人群差异化、禁共用句式"铁律，附参考范式
  - 场景描述：标注【A轨】，明确"画面→动作→感官→感受"四段递进结构及差异化要求
  - POD：由无语气要求改为标注【B轨 · 客观专业轨】，禁用主观感受词（"舒适安心""温润不烫""更到位"等），RTB 典籍原文保留但产品化小结句须收敛治疗性暗示（"止痛""疏通经络"→中性表述），契合无医疗宣称红线
  - FAQ：由"朋友聊天/拟人"语气改为标注【B轨 · 客观专业轨】，用事实数据、不拟人不画面化、禁忌类问题引注意事项原文并建议咨询医师
- 🎯 **来源**：蒸汽手腕带brief实战中用户拍板的风格口径（人群/场景=画面感口语化差异化；FAQ/POD=客观专业）固化入 Skill，供后续产品直接按此口径填写
- 📝 版本号：v1.6.2 → v1.7.0

## v1.6.2 (2026-07-06) — CDN 脚本懒加载，刷新速度翻倍

- 🚀 **性能优化**：html2canvas 和 jsPDF 从 `<head>` 同步加载改为懒加载
  - **根因**：每次刷新页面都要重新下载这两个合计数百KB的CDN脚本，在 WorkBuddy 内嵌 WebView 中尤其慢
  - **修复**：只在用户点击「📄 下载PDF」时才动态加载这两个库。页面加载/刷新不再需要等它们
  - 首次 PDF 导出会多等 1-2 秒（加载脚本），之后同一次会话中再次导出直接调用（已缓存）
- 📝 版本号：v1.6.1 → v1.6.2

## v1.6.1 (2026-07-06) — 产品口号写作手法扩充

- 🔧 **产品口号规范升级**：从2种类型（体验+结果、时令+功效）扩展为四大手法
  - 新增手法3「场景+画面」：用一句话构建使用场景的视觉想象
  - 新增手法4「数字对比」：用数字制造冲击力和可信度
  - 组合示例：`4小时温热 13味本草 一年就养这四十天`（数字+时令+画面三叠）
- 📝 版本号：v1.6.0 → v1.6.1

## v1.6.0 (2026-07-05) — 新增三伏贴文案参考 + 文案风格规范升级

- ✨ **新增外部参考案例**：三伏贴完整brief（`蔡氏福宁_brief_三伏贴_2026-07-05.md`），参考案例从2个扩充为3个
- 🔧 **文案风格规范升级**（基于三伏贴案例提炼的新模式）：
  - 产品口号：新增"时令概念+功效"型口号模式（`冬病夏治 三伏养阳`）vs 体验+结果型
  - 产品口播：新增三项进阶手法——时间窗口紧迫感、反常识对比、成分递进
  - 品类定义：模板升级为"外用 + 使用部位 + 核心机制 + 核心功能"，加入部位信息和时效限定
  - 人群描述：新增"标签化命名"手法（冰块族/堵堵族/易感族/脾胃弱族）和"现有方案的不足要具体"规则
  - 场景描述：新增三项进阶手法——反常识对比开场、季节与身体关联、穴位具名
- 🔧 资源列表：三个案例各标注独特的参考价值方向
- 📝 版本号：v1.5.1 → v1.6.0（中版本：文案参考体系扩充）

## v1.5.1 (2026-07-05) — 刷新按钮修复：缓存穿透

- 🐛 **修复**：「🔄 刷新数据」按钮使用 `location.reload()` 在内嵌 WebView（WorkBuddy 预览）中可能被缓存拦截，点击无反应
  - **根因**：`location.reload()` 可能从浏览器缓存加载而非重新读取磁盘文件
  - **修复**：改为 `location.href = url + '?_=' + Date.now()` —— 给 URL 加上变化的查询参数，强制浏览器绕过缓存重新请求
- 📝 版本号：v1.5.0 → v1.5.1

## v1.5.0 (2026-07-05) — 双通道数据注入（根治"注入成功但HTML空白"）

- 🔄 **架构级升级**：数据注入从单通道（localStorage）改为双通道
  - **通道1（主）**：`window.__BRIEF_DATA = {...}` — 直接在 HTML 中声明全局 JS 变量。完全绕过 localStorage
  - **通道2（备份）**：base64 → localStorage — 保留用于持久化
  - **根因**：之前所有数据都依赖 localStorage 加载。当浏览器环境不支持 localStorage（WorkBuddy 内嵌预览、隐私模式、沙箱 iframe）时，数据静默丢失。通道1 将数据作为 JS 代码直接嵌入 HTML，在任何环境中都能执行
- 🔧 `brief_inject.py`：preload 脚本重写为双通道
- 🔧 HTML init 代码：新增 `loadFromDirectData()` 备用加载器 + 双通道优先读取逻辑
- 🔧 SKILL.md 步骤C：移除"不要用内置预览"限制（双通道方案已解决此问题）
- 🔧 故障排查章节：简化排查清单，更新快速验证命令
- 📝 版本号：v1.4.1 → v1.5.0（中版本：架构级改动）

## v1.4.1 (2026-07-04) — 数据加载状态诊断 + 故障排查指引

- 🔧 **新增诊断横幅**：HTML 页面进度条下方显示数据加载状态
  - ✅ 绿色：已加载 N 个字段（来自 preload 注入 / localStorage）
  - ⚠️ 灰色：未检测到预加载数据，提示常见原因（文件不对/未刷新/内置预览）
  - ❌ 红色：preload 脚本执行失败（显示错误信息）或 JSON 异常
- 🔧 **preload 脚本状态标记**：成功加载时设置 `window.__brief_preload_ok=true` 和 `field_count`，失败时设置 `__brief_preload_error`
- 🔧 **SKILL.md 新增「故障排查」章节**：6 步排查清单 + 快速验证注入正确性的命令
- 🔧 **步骤C 输出模板升级**：明确标注 ⚠️ 不要用 WorkBuddy 内置预览面板打开（可能不支持 localStorage）→ 必须用外部浏览器
- 📝 版本号：v1.4.0 → v1.4.1

## v1.4.0 (2026-07-04) — 文案风格规范 + 缺失信息AI生成 + 合规规则

- ✨ **新增「文案风格规范」章节**：定义蔡氏福宁产品brief的统一语言基调和各版块写作模板
  - 参考案例：休足贴 brief + 蒸汽眼罩 brief（两个完整案例的行文风格、结构组织、用词习惯）
  - 语言基调：专业可信+亲切易懂，消费者"你"视角，高信息密度
  - 八个版块逐一给出结构模板、案例参考、禁止事项（口号/口播/品类定义/核心技术/人群/场景/POD/FAQ）
- ✨ **新增「缺失信息补充规则」章节**：当信息源未覆盖某字段时的处理策略
  - `📝AI参考` 机制：基于已确认产品事实 + 参考案例风格 → 生成参考方案供审核人确认
  - 事实锁：只能基于阶段0已确认的信息生成，不得引入新"事实"
  - 标注规则：所有 AI 生成内容加 `📝AI参考：` 前缀
  - 典型适用字段一览表
- ✨ **新增「合规规范：医疗功效暗示规避」章节**：确保所有文案符合广告法及日用品定位
  - 绝对禁用词清单（治疗/消炎/预防疾病/绝对化用语等 7 类）
  - 替代表述对照表（禁用→合规替代）
  - 合规自查话术模板（把"它治好了什么"变成"你感觉到了什么"）
  - 合规校验清单（5项逐条检查）
- 🔧 铁律声明：从两条扩展为三条，新增「合规是底线」铁律
- 🔧 禁止行为：新增 #12（AI参考不加标注）、#13（使用禁用词）、#14（AI参考跳过合规审查）
- 🔧 思考检查点：新增"AI参考已标注"和"合规自查"两条检查项
- 📝 版本号：v1.3.3 → v1.4.0（小版本→中版本：新增三个独立功能模块）

## v1.3.3 (2026-07-04) — 修复preload静默失败 + 清理逻辑竞态

- 🐛 **关键修复 #1**：`decodeURIComponent` hack 在特定字节序列上抛 `URIError`，导致 preload 脚本静默失败
  - **根因**：`decodeURIComponent` 对无效 UTF-8 字节序列抛异常（而非降级处理）。`atob` → binary string → `charCodeAt` → `%XX` → `decodeURIComponent` 这条链路中，某些合法 JSON 内容的字节组合可能触发此异常
  - **修复**：替换为 `TextDecoder("utf-8")` —— `atob()` → `Uint8Array` → `TextDecoder.decode()` → `JSON.parse`。`TextDecoder` 对无效字节序列降级为 U+FFFD 而非抛异常
  - 同时给 preload 脚本加 `try/catch` + `console.error`，失败不再静默
- 🐛 **关键修复 #2**：HTML 初始化清理逻辑与 preload 脚本存在竞态条件
  - **根因**：`DOMContentLoaded` 回调中 `removeItem('caishifuling_brief_v6')` 在未设置 `cleanup_done` 标记时执行——恰好抹掉 preload 脚本刚写入的数据（preload 在 DOMContentLoaded 之前运行）
  - **修复**：清理逻辑只 wipe v5，不再碰 v6
- 📝 SKILL.md 注入工作原理补充刷新说明

## v1.3.2 (2026-07-04) — 修复预览刷新引导

- 🔧 **关键修复**：SKILL.md 中 "实时查看填写效果" 表述误导——HTML 为磁盘静态文件，浏览器不会自动感知文件变更。改为明确指示审核人**手动刷新浏览器**（F5/Cmd+R）
- 🔧 SKILL.md 阶段1步骤C：确认提示从 "请打开该文件在浏览器中查看" 改为 "请**刷新浏览器页面**（F5 或 Cmd+R）"
- 🔧 SKILL.md 决策2：补齐 "⚠️ 审核人每次需手动刷新浏览器" 约束说明
- ✨ HTML工具工具栏新增「🔄 刷新数据」按钮（`location.reload()`），方便审核人一键刷新

## v1.3.1 (2026-07-03) — 修复注入脚本中文乱码

- 🐛 **关键修复**：`brief_inject.py` 注入的 `<script id="brief-preload">` 中 `atob()` 不解码 UTF-8，导致中文全部乱码
  - **根因**：`atob()` 返回 "binary string"（每字符1字节），但中文 UTF-8 是 3 字节/字符。`JSON.parse(atob(b64))` 将多字节序列当成 Latin-1 逐字符处理，产生乱码
  - **修复**：加入 `decodeURIComponent` hack —— 将 binary string 中每个字节转为 `%XX` 格式，再用 `decodeURIComponent` 还原为正确的 UTF-8 字符串，最后才 `JSON.parse`
- 🔧 `scripts/brief_inject.py` 第 71-85 行：注入脚本模板修改，新增注释说明乱码原因

## v1.3.0 (2026-07-03) — 架构修复：JSON + localStorage 脚本注入

- 🔄 **架构级修复**：写入方案从 perl DOM 修改改为 **JSON + localStorage 脚本注入**
  - **根因**：brief工具数据源是 localStorage（浏览器运行时），不是 HTML DOM。静态字段存在竞态条件，动态字段（成分/竞品/人群等7类）HTML源码容器为空——perl 永远匹配不到目标
  - **新方案**：AI 维护 `/tmp/brief_data.json`，每版块确认后用 `brief_inject.py` base64 编码注入 `<script id="brief-preload">`，页面加载时脚本自动预填 localStorage → `loadFromLocal()` 正常读取 → 所有字段正确渲染
- ✨ 新增 `scripts/brief_inject.py`：Python 注入脚本，处理 JSON → base64 → HTML 脚本标签的全流程
- 🔧 SKILL.md 阶段1步骤B完全重写：perl 正则替换 → Python JSON更新 + brief_inject.py 注入 + Python 验证
- 🔧 禁止行为更新：新增第11条「禁止用 perl 直接修改 HTML DOM」
- 📝 操作指南 v4.0：完整重写，新增数据流向图、JSON 数据格式、struct 计数规则、brief_inject.py 使用说明
- 📝 Alternatives Considered：新增决策6「为什么用 JSON + localStorage 替代 perl DOM 写入」
- 📝 阶段0 熔断器新增 JSON 初始化检查

## v1.2.0 (2026-07-03) — 修复沉默失败

- 🔧 **关键修复**：阶段1新增步骤C「grep验证写入结果」。每版块写入HTML后必须执行grep确认内容实际写入，**禁止**perl返回后直接声称成功。
- ⛔ **防沉默失败铁律**：`perl -i` 零匹配也返回exit 0，不加grep验证是内容空白的根本原因。
- 📝 禁止行为新增第10条：perl写入后不验证就直接声称成功
- 🔧 SKILL.md阶段1步骤循环从两步(A→B)升级为三步(A→B→C)
- 🔧 熔断器新增grep验证层（重试2次，降级为手动填写）
- 📝 操作指南新增「grep验证命令」节，含textarea/input/动态列表三种验证模板
- 📝 思考检查点更新：加入grep验证条目
- 📝 阶段2完整性锁更新：加入"经grep验证"约束

## v1.1.0 (2026-07-03) — 工作流重构

- 🔄 **工作流重构**：阶段0提前复制模板到工作区 → 阶段1逐版块确认后**即时写入HTML** → 阶段2简化为最终交付
- ✨ 新增 perl 命令模板供AI逐字段写入HTML（input/textarea/动态列表三种类型）
- 🔧 阶段2移除"生成MD文件"环节——HTML工具自带「导出Markdown」按钮，无需AI额外生成
- 🔧 禁止行为#8重写：从"禁止直接修改模板"更新为"禁止跳过逐版块写入"
- 📝 可逆性分级🟢扩展：新增「复制模板到工作区」「逐版块写入HTML字段值」为自主操作
- 🔴 可逆性分级🔴扩展：新增「修改Skill目录下的原始HTML模板」为完全禁止
- 📝 Alternatives Considered：决策2重写（逐版块写入 vs 最后统一填写），新增决策4（模板复制时机）
- 📝 操作指南 v3.0：新增完整的动态列表HTML模板和perl写入参考

## v1.0.2 (2026-07-03)

- 🔧 **修复模板污染**：阶段2强制复制模板到工作区，原始模板永不修改
- 🔧 **修复MD导入无反应**：HTML工具新增 `parseSimpleMarkdown()` 解析器，支持 `- key: value` AI生成格式
- ✨ HTML工具导入弹窗新增「📂 选择 .md 文件」按钮，支持文件上传导入
- ✨ `doImportMarkdown()` 双解析器自动切换：优先AI格式 → 回退原始格式
- 📝 SKILL.md 阶段0增加工作区确认；阶段2增加完整MD格式规范 + 导入提醒
- 🚫 禁止行为新增第8条：禁止直接修改模板HTML
- 📝 操作指南 v2.0：废弃方案B，推荐MD文件上传导入

## v1.0.1 (2026-06-30)

- 🔧 YAML补齐必填字段（emoji/trigger/author）
- 🔧 references/字段对照表硬编码路径改为变量占位 `[产品文件目录]`
- ✨ 新增「与其他Skill的关系」段，声明与文案审核Skill的互斥关系
- ✨ 新增「Alternatives Considered」段，记录3项关键设计决策
- ✨ 正文加入系统化五级标注（✅DO/☑SHOULD/✔MAY/⛔DO NOT），覆盖铁律/阶段/填写规则/联网搜索/操作方案
- ✨ 阶段1新增上步校验锁（🧷），阶段0未完成时阶段1不得启动
- 📝 末尾补设计者/开源协议声明

## v1.0.0 (2026-06-30)

- 初始版本：三阶段brief填写流程（确认信息源 → 逐版块对照填写 → 写入HTML输出）
- 八大版块字段对照（战略层/心智层/产品层/市场层/利益层·人群/利益层·场景/利益层·核心价值/常见问题解答）
- 填写规则：有据可依、无据留空、待搜索、逻辑校验、禁脑补
- 交叉验证规则：多源一致/有差异/单一来源三级处理
- 联网搜索规则：可搜索范围 + 审核人授权机制
- 可逆性分级🟢🟡🔴 + 禁止行为表（7项）
- 执行层机制：📦产出物锁（阶段0/1/2）+ 🔌熔断器（文件读取/联网搜索/HTML写入）+ 🔒完整性锁（5项）+ 🧠思考检查点（7项）
